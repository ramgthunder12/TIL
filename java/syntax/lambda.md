"클래스에 메소드가 한 개 정의되어 있을 때 사용할 수 있다"는 설명은 **틀림**.

람다식은 다음 조건에서 사용된다.

## ✔ 람다식

### 1) 함수형 인터페이스일 때 사용 가능
람다식은 **함수형 인터페이스**를 구현할 때,  
클래스를 직접 구현하거나 익명 클래스를 만들지 않고 간결하게 작성할 수 있는 문법이다.

#### 📌 함수형 인터페이스(Functional Interface)
- 추상 메서드를 **하나만** 가지고 있는 인터페이스  
- 예: Runnable, Callable, Comparator 등
- 객체(상태 + 행동) → 클래스(필드 + 메서드) 구조지만,  
  함수형 인터페이스는 **오직 하나의 행동(추상 메서드)** 만 표현하는 인터페이스

---

### 2) 람다식은 **타겟 타입(Target Type)** 으로 결정됨  
람다식은 그 자체로 타입을 갖지 않는다.  
**람다식이 배치되는 위치(문맥)가 요구하는 타입**으로 해석된다.

#### 📌 타겟 타입(Target Type)이란?
람다식이 대입되는 위치가 요구하는 타입을 의미한다.

타겟 타입이 되는 경우:
1. **변수에 대입될 때** → 그 변수의 타입이 타겟 타입  
2. **메서드 파라미터로 전달될 때** → 파라미터 타입이 타겟 타입  
3. **return 문에서 반환될 때** → 반환 타입이 타겟 타입

**예시**
```java
Runnable r = () -> System.out.println("run"); // 변수 타입이 Runnable → 람다 결정됨
button.addActionListener(e -> handle(e)); // 파라미터 타입이 ActionListener → 람다 결정됨
return (a, b) -> a + b; // return 타입이 FunctionalInterface → 람다 결정됨
```
