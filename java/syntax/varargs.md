# 가변 인자(Varargs)

가변 인자는 Java 5(JDK 1.5)에서 추가된 문법으로,  
**메서드가 개수가 정해지지 않은 인자(0개 이상)를 받을 수 있도록 하는 기능**이다.


## ✔ 1. 가변 인자 란

`Object... args` 처럼 `...` 문법을 사용해  
메서드 파라미터가 **가변 개수(variable number)** 의 인자를 받을 수 있도록 한다.

```
void print(Object... args) { }
```

## ✔ 2. 동작 방식
📌 정식 타입은 배열(Array Type)
Object... args 는 문법적으로는 가변 인자이지만,
JLS에서는 이것을 배열 타입(Object[]) 으로 본다.

📌 호출 시 동작 — 컴파일러가 자동으로 배열 생성
가변 인자 위치에 전달된 모든 인자들은
컴파일러가 자동으로 배열(new Object[]{…})로 변환하여 메서드에 전달한다.

```
print(1, 2, 3);
// 컴파일 변환
print(new Object[]{1, 2, 3});
```

## ✔ 3. 문법 규칙 (JLS 기반)
1) 반드시 마지막 파라미터에서만 사용 가능
```
void print(String label, Object... args) { } // OK
void print(Object... args, String label) { } // ❌ 컴파일 오류
```

> JLS : If the last formal parameter is a variable arity parameter …

2) 혼합 배열 표기(mixed array notation) 사용 금지

**선언	가능 여부**  

int[]... a	✔ 가능  
int[]... a[]	❌ 불가능  
int... a[]	❌ 불가능

> JLS : It is a compile-time error to use mixed array notation for a variable arity parameter.




## ✔ 4. 결론
가변 인자는 0개 이상 여러 개의 인자를 받을 수 있는 문법이다.

Object... args 와 같은 가변 인자 문법은 타입이 따로 존재하는 것이 아니라,
컴파일러가 전달된 인자를 배열(Object[])로 변환해 넘기는 방식이다.

가변 인자는 반드시 마지막 파라미터여야 하며,
혼합 배열 표기(mixed array notation)는 사용할 수 없다.

## ✔ 5. 용어 정리
📌 arity
“개수(count)”를 의미하는 학술 용어

파라미터 개수를 나타낼 때 사용: parameter arity = 파라미터 개수

📌 JLS (Java Language Specification)
자바 문법, 타입 규칙, 컴파일 규칙 등을 정의한 공식 언어 사양서
